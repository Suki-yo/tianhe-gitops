apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cookie-web
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Suki-yo/cookie
    targetRevision: HEAD
    path: k8s/cookie-web
  destination:
    server: https://kubernetes.default.svc
    namespace: cookie
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
